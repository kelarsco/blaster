{
  "name": "storereach",
  "version": "1.0.0",
  "private": true,
  "description": "wiblaster - Automated Store Email Extraction & Cold Outreach Tool",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client-wait\"",
    "dev:client": "cd client && npm run dev",
    "dev:client-wait": "wait-on http://localhost:4000 http://localhost:4001 -t 60000 && cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "start": "cd server && npm start",
    "postinstall": "cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}
