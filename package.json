{
  "name": "storereach",
  "version": "1.0.0",
  "private": true,
  "description": "wiblaster  Automated Store Email Extraction & Cold Outreach Tool",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client-wait\"",
    "dev:client": "cd client && npm run dev",
    "dev:client-wait": "wait-on http-get://127.0.0.1:4000/api/health http-get://127.0.0.1:4001/api/health http-get://127.0.0.1:4002/api/health -t 90000 && cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "postinstall": "npm install --prefix client",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "vercel-build": "npm run build:client",
    "start": "cd server && npm start"
   
    
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}
